//for loop for number array
var numbers = [45, 34, 12, 56, 12, 34, 56, 67, 24];

for (var i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}


//loop for printing array of items
var items = ['bottle', 'glass', 'sunglass', 'laptop', 'mug', 'packet'];

for (var i = 0; i < items.length; i++) {
    console.log("Item " + (i + 1) + ": " + items[i]);
}